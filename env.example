# ============================================================================
# Open Camp - Kids Camp Registration - Environment Configuration
# ============================================================================
# Copy this file to .dev.vars for local development
# DO NOT commit .dev.vars or any file containing secrets to version control
# ============================================================================

# ----------------------------------------------------------------------------
# CLOUDFLARE BINDINGS (configured in wrangler.toml)
# ----------------------------------------------------------------------------

# D1 Database
# Create with: npx wrangler d1 create open-camp-db
# Then add to wrangler.toml:
#   [[d1_databases]]
#   binding = "DB"
#   database_name = "open-camp-db"
#   database_id = "YOUR_DATABASE_ID"

# KV Namespace
# Create with: npx wrangler kv:namespace create KV
# Then add to wrangler.toml:
#   [[kv_namespaces]]
#   binding = "KV"
#   id = "YOUR_KV_NAMESPACE_ID"

# ----------------------------------------------------------------------------
# SECRETS (set via wrangler secret put)
# ----------------------------------------------------------------------------

# Resend API Key (optional - for email notifications)
# Get your key at: https://resend.com
# Set with: npx wrangler secret put RESEND_API_KEY
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ----------------------------------------------------------------------------
# KV VALUES (set via wrangler kv:key put)
# ----------------------------------------------------------------------------

# Admin credentials
# Set with: npx wrangler kv:key put --binding=KV "admin_username" "admin"
# Set password hash with: 
#   echo -n 'your-password' | shasum -a 256 | cut -d' ' -f1
#   npx wrangler kv:key put --binding=KV "admin_password_hash" "YOUR_HASH"

# Max registration spots
# Set with: npx wrangler kv:key put --binding=KV "max_spots" "20"

# ----------------------------------------------------------------------------
# ENVIRONMENT VARIABLES (set in wrangler.toml [vars])
# ----------------------------------------------------------------------------

# CORS allowed origin (your Pages domain)
ALLOWED_ORIGIN=https://your-project.pages.dev
